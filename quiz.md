Why do we need to `import React from 'react'` in our files?
  React is what defines JSX  or
  Becus the JSX syntax is define in our react 

If i were to console.log(page) in index.js, what would show up?
  A Javascript object. React elements that describe what React should eventually add to the real DOM for us

What does it mean for something to be 'declarative' instead or 'imparative'?
  declarative means i can tell the computer what to do and expect it to handle the details.
  imparative means i need to tell it How to do each steps

what does it mean for something to be 'composable'?
  we have small pieces that can put together to make something larger/greater than the individual pieces.

what is React component
  components are independent and reusable bits of code or
  a function that returns react elements. (UI)

What do props help us acomplish?
  when it is pass it makes a component more reusable.

How do u pass a prop into a component
  <img src ='' />